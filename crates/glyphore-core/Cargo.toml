[package]
name = "glyphore-core"
description = "Deterministic MapLibre glyph PBF (SDF) generation — core logic"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
fontdue = "0.9"
sdf_glyph_renderer = { version = "1", default-features = false }
thiserror = "2"
ttf-parser = "0.25"
