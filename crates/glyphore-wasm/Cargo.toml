[package]
name = "glyphore-wasm"
description = "Deterministic MapLibre glyph PBF (SDF) generation — wasm-bindgen bindings"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
glyphore-core = { path = "../glyphore-core" }
js-sys = "=0.3.102"
wasm-bindgen = "=0.2.125"
